package com.example.drivinglicenseexamnepal_.data.constants

enum class Mode {
    QUIZ_MODE,
    STUDY_MODE,
    RESULT_MODE
}

object VehicleType {
    const val BIKE = "bike"
    const val CAR = "car"
}